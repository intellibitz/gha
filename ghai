#!/usr/bin/env bash
# 🤖 ghai - Bare minimum 100% Kotlin pass-through launcher
# Zero shell logic to prevent platform dependency traps.
chmod +x gradlew 2>/dev/null || true
exec ./gradlew --init-script init/gha.init.gradle.kts ghai "$@"
